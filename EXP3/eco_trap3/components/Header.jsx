
const Header = ({title}) => {
    return (
        <header style={{backgroundColor: "#f0ff67ff", height: 'auto', 
        borderRadius: 10, paddingLeft: 10}}>
            <h1>{title}</h1>
        </header>
    )
}

export default Header;